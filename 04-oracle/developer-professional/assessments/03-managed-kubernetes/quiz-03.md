# Assessment 03: Managed Kubernetes

> Generated by lesson-eval (coverage mode) on 2026-07-23. Source spec: STUDY-PLAN.md entry for
> module 03 + `../../../GUIDELINES.md` (threads: *exam-ready* / *production-reasoning*).
> Caveat: quiz author and lesson author are the same model in the same session — blindness is
> procedural (questions derived from the spec before re-reading the lesson body), not absolute.
> Diversified from `quiz-01.md`/`quiz-02.md`: probes boundary cases, "why NOT" questions, and
> whether the dual exam-ready/production-reasoning thread is visibly supported rather than
> re-testing the same core mechanics.

## Questions

1. A compliance requirement mandates a contractual uptime guarantee on the Kubernetes API server. Does Basic satisfy this if the team just monitors uptime closely themselves? _(tier SLA boundary · exam-ready)_
2. Is there any way to move an Enhanced cluster back to Basic to save cost after a project's SLA requirement goes away? _(upgrade path boundary · exam-ready)_
3. A workload needs a GPU-backed compute shape. Can it run on a virtual node? _(virtual node shape boundary · production-reasoning)_
4. A managed node pool has three nodes each with 4 vCPUs free, and a new pod requests 2 vCPUs. Does the Cluster Autoscaler get involved in placing it? _(scaling threshold / headroom model · exam-ready)_
5. You hold full "manage clusters" IAM policy but have no VCN connectivity to a private-endpoint cluster. Can you run `kubectl` commands against it? _(IAM vs. network layering · production-reasoning)_
6. Can OSOK provision an OCI Streaming resource or a Vault secret the same way it provisions an Autonomous Database? _(OSOK resource-type boundary · exam-ready)_
7. Redo the six-pod scale-up walkthrough assuming `orders-service` runs on a virtual-node pool instead of a managed one. Which steps disappear entirely? _(scaling walkthrough / virtual node contrast · production-reasoning)_
8. A control plane sits at v1.32 and a node pool sits at v1.28 — four minor versions behind. Is this within the skew policy? _(skew policy arithmetic · exam-ready)_
9. What makes OKE's control plane "IAM-native" in a way that's specific to OCI rather than universal to any managed Kubernetes control plane? _(OKE-specific IAM governance · exam-ready)_
10. Why can't a virtual-node pod use a `PersistentVolumeClaim` for durable storage — connect the answer to the resource-allocation model, not just "it's unsupported." _(virtual node PVC restriction / mechanism · production-reasoning)_
11. A team's Enhanced cluster runs 3,000 managed nodes on VCN-Native pod networking. Does the lesson's stated ceiling support this out of the box? _(node ceiling by networking mode · exam-ready)_
12. Which single fact in this lesson reads as pure exam trivia with no real operational consequence if forgotten, and which trade-off would you still need to reason about even with no exam at all? _(exam-ready vs. production-reasoning thread · production-reasoning)_

## Answer key

1. No — Basic carries no SLA at all on API server uptime; only Enhanced's SLA is financially backed, and no amount of self-monitoring substitutes for a contractual guarantee Basic doesn't offer. — **Covered**: §2.1 table, and §2.3 selection guidance ("a compliance requirement for an uptime guarantee... is in scope" → reach for Enhanced).
2. No — the reverse direction does not exist at all; an Enhanced cluster can never be downgraded to Basic. — **Covered**: §2.2.
3. Not stated directly. — **Partial**: §3.5 implies it by naming GPU/bare-metal shape needs as a reason to reach for managed nodes, but the lesson never states as a direct fact that virtual nodes categorically exclude GPU or specialized shapes — a reader has to infer the negative from the positive selection guidance for managed nodes rather than being told outright.
4. No — the scheduler places the pod directly into the existing node's free headroom; the Autoscaler only acts once no node has enough free capacity and pods sit `Pending`. — **Covered** (by combining two stated mechanics): §3.2 establishes headroom-based scheduling on managed nodes, and §5.4 steps 2–3 establish that the Autoscaler activates specifically on the unschedulable-`Pending` condition, not on every scheduling event.
5. No — IAM policy and network reachability are separate requirements; a private-endpoint cluster still needs a real VCN network path regardless of what IAM allows. — **Covered**: §4.1, and §4.3's Nuance stated for Cloud Shell specifically but generalizing to any external caller.
6. Not stated. — **Partial**: §6.1 names Autonomous Database and MySQL HeatWave "and others" without enumerating the full supported set — an honestly-disclosed non-exhaustive list, but a reader asking specifically about Streaming or Vault is left unanswered either way.
7. Steps 2–4 (scheduling failure, the Autoscaler reacting, a new node joining) would not occur at all — each new pod would provision its own execution slot directly, with no unschedulable state and no node to add. — **Covered**: §5.4, closing line.
8. No — from Kubernetes 1.28 onward the control plane may run up to three minor versions ahead of a node pool; four minor versions exceeds that window, so this pool is out of skew and must catch up. — **Covered** (the rule is stated in §5.1; applying it to this specific gap is the reader's arithmetic, in keeping with an apply-style question).
9. The control plane is a first-class OCI resource with its own OCID sitting inside your compartment, governed by ordinary IAM policy — the same policy model that governs every other OCI resource, rather than a bespoke Kubernetes-native RBAC layer bolted on separately. — **Covered**: §1.1.
10. A virtual node has no persistent machine behind it at all from your side — pods are per-pod execution slots Oracle provisions and bills individually, not attached to a durable, addressable node — so there is no stable place for a PVC to bind a volume to across the pod's lifecycle. — **Covered**: §3.1 ("A virtual node has no such machine at all from your side") connected to the restriction stated in §3.3.
11. No — the lesson's own stated ceiling caps VCN-Native pod networking at 2,000 managed nodes per Enhanced cluster, below the 3,000 in this scenario; only the Flannel path reaches 5,000 (20,000 with approval). — **Covered (stale)**: the reasoning is sound and matches §7's stated figures, but the exact node-ceiling numbers in §7 could not be independently re-confirmed against the live OCI service-limits page this run (see Currency verification) — route this fact to re-verification before treating the 2,000 figure as current.
12. No single universally-right answer expected — used to check the dual thread is visibly supported rather than asserted. — **Covered**: the lesson's Selection-guidance subsections (§2.3, §3.5), Nuance callouts, and Practical Limits section consistently pair an exam-ready fact with the production-reasoning trade-off behind it — e.g. §2.1's price/SLA figures are exam trivia on their own, but the Nuance directly after them ("a feature gate, not a price tag") is the reasoning a reader needs regardless of any exam.

## Coverage summary

9/12 covered · 2 partial · 0 missing (1 of the 9 "Covered" is tagged **Covered (stale)** — see Q11).

Ranked gaps (most to least important):
1. (Q11 — stale) The §7 node-ceiling figures (1,000 Basic / up to 5,000–20,000 Enhanced-Flannel / 2,000 VCN-Native / 256 pods per node / 1,000 nodes per pool) could not be independently re-confirmed this run — the live OCI service-limits page was unreachable. The lesson already tags these with an as-of date and doc link (correct per the track's volatile-facts policy), so this is a re-verification task, not a lesson defect.
2. (Q3) Virtual nodes' exclusion of GPU/specialized shapes is inferable from the managed-node selection guidance but never stated as a direct virtual-node limitation.
3. (Q6) OSOK's supported-resource-type list is an honestly-disclosed "and others," but leaves specific reader questions (Streaming? Vault?) unanswered either way.

Unverifiable this run: the §7 node/pod/pool ceiling figures and the virtual-node-pools-per-region quota (3 Pay As You Go / 9 Universal Credits) — the official `servicelimits.htm` page returned 403/404 on repeated fetch attempts during this session's currency check; the general shape (Enhanced scales far higher than Basic, VCN-Native has a lower default ceiling than Flannel) is corroborated by other current Oracle sources, but the exact figures should be re-checked directly against the live docs page.

## Resolved

2026-07-23 — One gap closed, one reverified, one left open:
- **Q3** (GPU/specialized shapes on virtual nodes): added directly to §3.3 as an explicit exclusion, confirmed against Oracle's virtual-vs-managed-node comparison docs — no longer left as an inference from the managed-node selection guidance alone.
- **Q11 — reverified, not stale**: a later fetch of the live OCI service-limits page succeeded and *confirmed* the lesson's §7 figures exactly as written (1,000 Basic / 5,000–20,000 Enhanced-Flannel / 2,000 VCN-Native / 256 pods per node / 1,000 nodes per pool / 3 PAYG–9 Universal-Credits virtual node pools per region). The **Covered (stale)** verdict recorded above reflects what was knowable at grading time; no lesson edit was needed since the figures were already correct.
- **Q6** (OSOK resource-type completeness) was also closed in this pass, though it surfaced independently rather than from this quiz's own ranked list: §6.1 now names OCI Streaming and OCI Queue alongside Autonomous Database and MySQL HeatWave, confirmed against the OSOK project's own documentation index.
